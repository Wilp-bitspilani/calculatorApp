apiVersion: apps/v1
kind: Deployment
metadata:
  name: Calculator
  replicas: 1
  selector:
    matchLabels:
      app: Calculator
  template:
    metadata:
      labels:
        app: Calculator
    spec:
      containers:
        - name: calculator-container
          image: your-docker-image:latest
          ports:
            - containerPort: 80